
=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/index.html"
Description: "Add volume controls UI to index.html in the admin panel"
=================================
--- TARGET ---
"            <div style=\"display:flex; align-items:center; gap:6px;\">\n                <div class=\"status-dot\" id=\"statusDotTT\"></div>\n                <span id=\"statusTextTT\" style=\"font-size:11px;\">TT: —</span>\n            </div>\n\n        </div>\n        \n        <!-- Music Player -->"
+++ REPLACEMENT +++
"            <div style=\"display:flex; align-items:center; gap:6px;\">\n                <div class=\"status-dot\" id=\"statusDotTT\"></div>\n                <span id=\"statusTextTT\" style=\"font-size:11px;\">TT: —</span>\n            </div>\n\n        </div>\n        \n        <!-- Volume Controls -->\n        <div id=\"volumeControlsUI\" style=\"background: rgba(0, 0, 0, 0.4); padding: 10px; border-radius: 6px; margin-bottom: 10px; display: flex; flex-direction: column; gap: 8px;\">\n            <div style=\"font-size: 12px; font-weight: bold; text-align: center; color: #ffdd44; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 4px;\">🔊 CONTROLE DE VOLUME</div>\n            \n            <div style=\"display: flex; flex-direction: column; gap: 2px;\">\n                <div style=\"display: flex; justify-content: space-between; font-size: 10px; color: #aaa;\">\n                    <span>🎮 Sons do Jogo</span>\n                    <span id=\"gameVolumeVal\">50%</span>\n                </div>\n                <input type=\"range\" id=\"volGame\" min=\"0\" max=\"100\" value=\"50\" style=\"width: 100%; cursor: pointer;\" oninput=\"changeVolume('game', this.value)\">\n            </div>\n            \n            <div style=\"display: flex; flex-direction: column; gap: 2px;\">\n                <div style=\"display: flex; justify-content: space-between; font-size: 10px; color: #aaa;\">\n                    <span>🎵 Música MP3</span>\n                    <span id=\"musicVolumeVal\">50%</span>\n                </div>\n                <input type=\"range\" id=\"volMusic\" min=\"0\" max=\"100\" value=\"50\" style=\"width: 100%; cursor: pointer;\" oninput=\"changeVolume('music', this.value)\">\n            </div>\n            \n            <div style=\"display: flex; flex-direction: column; gap: 2px;\">\n                <div style=\"display: flex; justify-content: space-between; font-size: 10px; color: #aaa;\">\n                    <span>💬 Mensagens MP3 / Voz</span>\n                    <span id
<truncated 318 bytes>

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Add global volume variables, functions and bridge in app.js"
=================================
--- TARGET ---
"        var lastHeartDamage = {}; // Tracker para cooldown de dano (0.5 hearts)\n\n        const INITIAL_HEART_SLOTS = 3;"
+++ REPLACEMENT +++
"        var lastHeartDamage = {}; // Tracker para cooldown de dano (0.5 hearts)\n\n        // --- Volume Settings & Audio Registry ---\n        var gameSoundsVolume = parseFloat(localStorage.getItem('pd_gamesounds_volume') || '0.5');\n        var musicVolume = parseFloat(localStorage.getItem('pd_music_volume') || '0.5');\n        var messagesVolume = parseFloat(localStorage.getItem('pd_messages_volume') || '0.5');\n        var allSFXElements = [];\n        var gameSoundsGainNode = null;\n        var musicGainNode = null;\n\n        function createSFX(src, baseVol) {\n            var audio = new Audio(src);\n            audio.baseVolume = baseVol !== undefined ? baseVol : 1.0;\n            audio.volume = audio.baseVolume * gameSoundsVolume;\n            allSFXElements.push(audio);\n            return audio;\n        }\n\n        function createSFXArray(sources, baseVol) {\n            return sources.map(function(src) {\n                return createSFX(src, baseVol);\n            });\n        }\n\n        function cloneSFX(audioElement) {\n            var cloned = audioElement.cloneNode();\n            cloned.baseVolume = audioElement.baseVolume !== undefined ? audioElement.baseVolume : 1.0;\n            cloned.volume = cloned.baseVolume * gameSoundsVolume;\n            return cloned;\n        }\n\n        function updateGameSoundsVolume(newVolume) {\n            gameSoundsVolume = newVolume;\n            localStorage.setItem('pd_gamesounds_volume', newVolume);\n            allSFXElements.forEach(function(audio) {\n                audio.volume = audio.baseVolume * gameSoundsVolume;\n            });\n            if (gameSoundsGainNode) {\n                gameSoundsGainNode.gain.value = gameSoundsVolume;\n            }\n        }\n\n        function updateMusicVolume(newVolume) {\n            musicVolume = newVolume;\n            localStorage.setItem('pd_music_volume', newVolume);\n            if (musicGainNode) {\n                musicGainNode.gain.value = musicVolume;\n            }\n        }\n\n        function
<truncated 971 bytes>

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Initialize volume UI sliders on DOMContentLoaded in app.js"
=================================
--- TARGET ---
"        window.addEventListener('DOMContentLoaded', function () {\n            window.updateUIMode();\n        });"
+++ REPLACEMENT +++
"        window.addEventListener('DOMContentLoaded', function () {\n            window.updateUIMode();\n\n            var volGameInput = document.getElementById('volGame');\n            var volMusicInput = document.getElementById('volMusic');\n            var volMessagesInput = document.getElementById('volMessages');\n\n            if (volGameInput) {\n                volGameInput.value = Math.round(gameSoundsVolume * 100);\n                var el = document.getElementById('gameVolumeVal');\n                if (el) el.textContent = volGameInput.value + '%';\n            }\n            if (volMusicInput) {\n                volMusicInput.value = Math.round(musicVolume * 100);\n                var el = document.getElementById('musicVolumeVal');\n                if (el) el.textContent = volMusicInput.value + '%';\n            }\n            if (volMessagesInput) {\n                volMessagesInput.value = Math.round(messagesVolume * 100);\n                var el = document.getElementById('messagesVolumeVal');\n                if (el) el.textContent = volMessagesInput.value + '%';\n            }\n        });"

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Register chickenSounds, homerSound, and coinSound with the volume manager"
=================================
--- TARGET ---
"        var chickenSounds = [\n            new Audio('sons/galinha um.ogg'),\n            new Audio('sons/galinha dois.ogg')\n        ];\n        var homerSound = new Audio('homer/homer.mp3');\n        var coinSound = new Audio('sons/coin sound.mp3');"
+++ REPLACEMENT +++
"        var chickenSounds = createSFXArray([\n            'sons/galinha um.ogg',\n            'sons/galinha dois.ogg'\n        ]);\n        var homerSound = createSFX('homer/homer.mp3');\n        var coinSound = createSFX('sons/coin sound.mp3');"

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Register dragon roar and flap sounds with the volume manager"
=================================
--- TARGET ---
"            for (var i = 1; i <= 4; i++) dragonRoarSounds.push(new Audio('sons/dragon_idle' + i + '.ogg'));\n            for (var i = 1; i <= 6; i++) dragonFlapSounds.push(new Audio('sons/dragon_flap' + i + '.ogg'));"
+++ REPLACEMENT +++
"            for (var i = 1; i <= 4; i++) dragonRoarSounds.push(createSFX('sons/dragon_idle' + i + '.ogg'));\n            for (var i = 1; i <= 6; i++) dragonFlapSounds.push(createSFX('sons/dragon_flap' + i + '.ogg'));"

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Register rest of game sounds and update cloning logic in play animal functions"
=================================
--- TARGET ---
"        var swordTransitionSound = new Audio('sons/espada.mp3');\n        function playSwordTransition() { if (_audioStarted) { swordTransitionSound.currentTime = 0; swordTransitionSound.play().catch(function (e) { }); } }\n        var swordHitSound = new Audio('sons/ataque de espada.ogg');\n\n        // Combo Kill Sounds\n        var sfxDoubleKill = new Audio('sons kills/double-kill.mp3');\n        var sfxTripleKill = new Audio('sons kills/triple-kill.mp3');\n        var sfxUltraKill = new Audio('sons kills/ultra-kill.mp3');\n        var sfxMegaKill = new Audio('sons kills/mega-kill.mp3');\n        var sfxMonsterKill = new Audio('sons kills/ut-monster-kill_1iyCEdo.mp3');\n\n        function playComboKillSound(comboCount) {\n            if (!_audioStarted) return;\n            var snd = null;\n            if (comboCount === 2) snd = sfxDoubleKill;\n            else if (comboCount === 3) snd = sfxTripleKill;\n            else if (comboCount === 4) snd = sfxUltraKill;\n            else if (comboCount === 5) snd = sfxMegaKill;\n            else if (comboCount >= 6) snd = sfxMonsterKill;\n\n            if (snd) {\n                snd.currentTime = 0;\n                snd.play().catch(function (e) { console.error('Play combo sound error:', e); });\n            }\n        }\n\n        // Clone and Thor Sounds\n        var cloneSound = new Audio('sons/clone.mp3');\n        function playCloneSound() { if (_audioStarted) { cloneSound.currentTime = 0; cloneSound.play().catch(function (e) {}); } }\n\n        var thorSound = new Audio('sons/thor.mp3');\n        function playThorSound() { if (_audioStarted) { thorSound.currentTime = 0; thorSound.play().catch(function (e) {}); } }\n        var sfxSpiderHurt = new Audio('enemy/Spider.ogg');\n        var sfxSpiderDeath = new Audio('enemy/Spider_death.ogg');\n        function playSpiderHurt() { if (_audioStarted) { sfxSpiderHurt.currentTime = 0; sfxSpiderHurt.play().catch(function (e) { }); } }\n        function playSpiderDeath() { if (_audioStarted) { sfxSpiderDeath.currentTime
<truncated 3693 bytes>
+++ REPLACEMENT +++
"        var swordTransitionSound = createSFX('sons/espada.mp3');\n        function playSwordTransition() { if (_audioStarted) { swordTransitionSound.currentTime = 0; swordTransitionSound.play().catch(function (e) { }); } }\n        var swordHitSound = createSFX('sons/ataque de espada.ogg');\n\n        // Combo Kill Sounds\n        var sfxDoubleKill = createSFX('sons kills/double-kill.mp3');\n        var sfxTripleKill = createSFX('sons kills/triple-kill.mp3');\n        var sfxUltraKill = createSFX('sons kills/ultra-kill.mp3');\n        var sfxMegaKill = createSFX('sons kills/mega-kill.mp3');\n        var sfxMonsterKill = createSFX('sons kills/ut-monster-kill_1iyCEdo.mp3');\n\n        function playComboKillSound(comboCount) {\n            if (!_audioStarted) return;\n            var snd = null;\n            if (comboCount === 2) snd = sfxDoubleKill;\n            else if (comboCount === 3) snd = sfxTripleKill;\n            else if (comboCount === 4) snd = sfxUltraKill;\n            else if (comboCount === 5) snd = sfxMegaKill;\n            else if (comboCount >= 6) snd = sfxMonsterKill;\n\n            if (snd) {\n                snd.currentTime = 0;\n                snd.play().catch(function (e) { console.error('Play combo sound error:', e); });\n            }\n        }\n\n        // Clone and Thor Sounds\n        var cloneSound = createSFX('sons/clone.mp3');\n        function playCloneSound() { if (_audioStarted) { cloneSound.currentTime = 0; cloneSound.play().catch(function (e) {}); } }\n\n        var thorSound = createSFX('sons/thor.mp3');\n        function playThorSound() { if (_audioStarted) { thorSound.currentTime = 0; thorSound.play().catch(function (e) {}); } }\n        var sfxSpiderHurt = createSFX('enemy/Spider.ogg');\n        var sfxSpiderDeath = createSFX('enemy/Spider_death.ogg');\n        function playSpiderHurt() { if (_audioStarted) { sfxSpiderHurt.currentTime = 0; sfxSpiderHurt.play().catch(function (e) { }); } }\n        function playSpiderDeath() { if (_audioStarted) { sfxSpiderDeath.currentTime
<truncated 3911 bytes>

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Implement gameSoundsGainNode and musicGainNode in getAC and split reverb nodes"
=================================
--- TARGET ---
"        var AC = null;\n        function getAC() {\n            if (!AC) AC = new (window.AudioContext || window.webkitAudioContext)();\n            if (AC.state === 'suspended') AC.resume();\n            return AC;\n        }\n\n        var _reverbNode = null;\n        function getReverbNode() {\n            var ac = getAC();\n            if (_reverbNode && _reverbNode.context === ac) return _reverbNode;\n            var len = ac.sampleRate * 2.2;\n            var buf = ac.createBuffer(2, len, ac.sampleRate);\n            for (var ch = 0; ch < 2; ch++) {\n                var d = buf.getChannelData(ch);\n                for (var i = 0; i < len; i++) d[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, 2.5);\n            }\n            var conv = ac.createConvolver(); conv.buffer = buf;\n            var wetG = ac.createGain(); wetG.gain.value = 0.38;\n            conv.connect(wetG); wetG.connect(ac.destination);\n            _reverbNode = conv; return conv;\n        }\n\n        function playNote(freq, type, vol, attack, decay, when) {\n            var ac = getAC();\n            var osc = ac.createOscillator();\n            var gain = ac.createGain();\n            var rev = getReverbNode();\n            osc.connect(gain); gain.connect(ac.destination); gain.connect(rev);"
+++ REPLACEMENT +++
"        var AC = null;\n        function getAC() {\n            if (!AC) {\n                AC = new (window.AudioContext || window.webkitAudioContext)();\n                \n                gameSoundsGainNode = AC.createGain();\n                gameSoundsGainNode.gain.value = gameSoundsVolume;\n                gameSoundsGainNode.connect(AC.destination);\n                \n                musicGainNode = AC.createGain();\n                musicGainNode.gain.value = musicVolume;\n                musicGainNode.connect(AC.destination);\n            }\n            if (AC.state === 'suspended') AC.resume();\n            return AC;\n        }\n\n        var _sfxReverbNode = null;\n        function getSFXReverbNode() {\n            var ac = getAC();\n            if (_sfxReverbNode && _sfxReverbNode.context === ac) return _sfxReverbNode;\n            var len = ac.sampleRate * 2.2;\n            var buf = ac.createBuffer(2, len, ac.sampleRate);\n            for (var ch = 0; ch < 2; ch++) {\n                var d = buf.getChannelData(ch);\n                for (var i = 0; i < len; i++) d[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, 2.5);\n            }\n            var conv = ac.createConvolver(); conv.buffer = buf;\n            var wetG = ac.createGain(); wetG.gain.value = 0.38;\n            conv.connect(wetG); wetG.connect(gameSoundsGainNode);\n            _sfxReverbNode = conv; return conv;\n        }\n\n        var _musicReverbNode = null;\n        function getMusicReverbNode() {\n            var ac = getAC();\n            if (_musicReverbNode && _musicReverbNode.context === ac) return _musicReverbNode;\n            var len = ac.sampleRate * 2.2;\n            var buf = ac.createBuffer(2, len, ac.sampleRate);\n            for (var ch = 0; ch < 2; ch++) {\n                var d = buf.getChannelData(ch);\n                for (var i = 0; i < len; i++) d[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, 2.5);\n            }\n            var conv = ac.createConvolver(); conv.buffer = buf;\n            var wetG = ac.c
<truncated 574 bytes>

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Route sfxTormenta Web Audio nodes to gameSoundsGainNode"
=================================
--- TARGET ---
"            sub.connect(subG); subG.connect(ac.destination); sub.start(t); sub.stop(t + 1.6);\n            for (var i = 0; i < 6; i++) {\n                playNote(220 * Math.pow(1.5, i), 'sawtooth', 0.08, 0.01, 0.2, t + i * 0.08);\n            }\n            var nLen = Math.ceil(ac.sampleRate * 0.8);\n            var buf = ac.createBuffer(1, nLen, ac.sampleRate);\n            var d = buf.getChannelData(0);\n            for (var j = 0; j < nLen; j++) d[j] = (Math.random() * 2 - 1) * Math.pow(1 - j / nLen, 1.2);\n            var src = ac.createBufferSource(); src.buffer = buf;\n            var filt = ac.createBiquadFilter(); filt.type = 'lowpass'; filt.frequency.value = 600;\n            var ng = ac.createGain(); ng.gain.setValueAtTime(0.4, t);\n            ng.gain.exponentialRampToValueAtTime(0.0001, t + 0.8);\n            src.connect(filt); filt.connect(ng); ng.connect(ac.destination); src.start(t);"
+++ REPLACEMENT +++
"            sub.connect(subG); subG.connect(gameSoundsGainNode); sub.start(t); sub.stop(t + 1.6);\n            for (var i = 0; i < 6; i++) {\n                playNote(220 * Math.pow(1.5, i), 'sawtooth', 0.08, 0.01, 0.2, t + i * 0.08);\n            }\n            var nLen = Math.ceil(ac.sampleRate * 0.8);\n            var buf = ac.createBuffer(1, nLen, ac.sampleRate);\n            var d = buf.getChannelData(0);\n            for (var j = 0; j < nLen; j++) d[j] = (Math.random() * 2 - 1) * Math.pow(1 - j / nLen, 1.2);\n            var src = ac.createBufferSource(); src.buffer = buf;\n            var filt = ac.createBiquadFilter(); filt.type = 'lowpass'; filt.frequency.value = 600;\n            var ng = ac.createGain(); ng.gain.setValueAtTime(0.4, t);\n            ng.gain.exponentialRampToValueAtTime(0.0001, t + 0.8);\n            src.connect(filt); filt.connect(ng); ng.connect(gameSoundsGainNode); src.start(t);"

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Route sfxPop Web Audio nodes to gameSoundsGainNode"
=================================
--- TARGET ---
"                o.connect(g); g.connect(ac.destination);"
+++ REPLACEMENT +++
"                o.connect(g); g.connect(gameSoundsGainNode);"

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Route bat sounds to gameSoundsGainNode"
=================================
--- TARGET ---
"        function sfxBatHurt() {\n            if (!_audioStarted) return;\n            var ac = getAC(), t = ac.currentTime;\n            var o = ac.createOscillator(), g = ac.createGain();\n            o.type = 'sine';\n            o.frequency.setValueAtTime(600, t);\n            o.frequency.exponentialRampToValueAtTime(300, t + 0.08);\n            g.gain.setValueAtTime(0.1, t);\n            g.gain.exponentialRampToValueAtTime(0.0001, t + 0.1);\n            o.connect(g); g.connect(ac.destination);\n            o.start(t); o.stop(t + 0.1);\n        }\n\n        function sfxBatDeath() {\n            if (!_audioStarted) return;\n            var ac = getAC(), t = ac.currentTime;\n            // Bubble pop sound\n            var o = ac.createOscillator(), g = ac.createGain();\n            o.type = 'sine';\n            o.frequency.setValueAtTime(800, t);\n            o.frequency.exponentialRampToValueAtTime(200, t + 0.12);\n            g.gain.setValueAtTime(0.15, t);\n            g.gain.exponentialRampToValueAtTime(0.0001, t + 0.15);\n            o.connect(g); g.connect(ac.destination);\n            o.start(t); o.stop(t + 0.15);\n            // Tiny noise burst\n            var nLen = Math.ceil(ac.sampleRate * 0.05);\n            var buf = ac.createBuffer(1, nLen, ac.sampleRate);\n            var d = buf.getChannelData(0);\n            for (var j = 0; j < nLen; j++) d[j] = (Math.random() * 2 - 1) * (1 - j / nLen);\n            var src = ac.createBufferSource(); src.buffer = buf;\n            var ng = ac.createGain(); ng.gain.setValueAtTime(0.08, t); ng.gain.exponentialRampToValueAtTime(0.0001, t + 0.05);\n            src.connect(ng); ng.connect(ac.destination); src.start(t);\n        }"
+++ REPLACEMENT +++
"        function sfxBatHurt() {\n            if (!_audioStarted) return;\n            var ac = getAC(), t = ac.currentTime;\n            var o = ac.createOscillator(), g = ac.createGain();\n            o.type = 'sine';\n            o.frequency.setValueAtTime(600, t);\n            o.frequency.exponentialRampToValueAtTime(300, t + 0.08);\n            g.gain.setValueAtTime(0.1, t);\n            g.gain.exponentialRampToValueAtTime(0.0001, t + 0.1);\n            o.connect(g); g.connect(gameSoundsGainNode);\n            o.start(t); o.stop(t + 0.1);\n        }\n\n        function sfxBatDeath() {\n            if (!_audioStarted) return;\n            var ac = getAC(), t = ac.currentTime;\n            // Bubble pop sound\n            var o = ac.createOscillator(), g = ac.createGain();\n            o.type = 'sine';\n            o.frequency.setValueAtTime(800, t);\n            o.frequency.exponentialRampToValueAtTime(200, t + 0.12);\n            g.gain.setValueAtTime(0.15, t);\n            g.gain.exponentialRampToValueAtTime(0.0001, t + 0.15);\n            o.connect(g); g.connect(gameSoundsGainNode);\n            o.start(t); o.stop(t + 0.15);\n            // Tiny noise burst\n            var nLen = Math.ceil(ac.sampleRate * 0.05);\n            var buf = ac.createBuffer(1, nLen, ac.sampleRate);\n            var d = buf.getChannelData(0);\n            for (var j = 0; j < nLen; j++) d[j] = (Math.random() * 2 - 1) * (1 - j / nLen);\n            var src = ac.createBufferSource(); src.buffer = buf;\n            var ng = ac.createGain(); ng.gain.setValueAtTime(0.08, t); ng.gain.exponentialRampToValueAtTime(0.0001, t + 0.05);\n            src.connect(ng); ng.connect(gameSoundsGainNode); src.start(t);\n        }"

=================================
Tool: replace_file_content | Target: "c:/laragon8/www/fallingpickaxeticktockmoney - Copia/js/app.js"
Description: "Route procedural game sounds to gameSoundsGainNode"
=================================
--- TARGET ---
"        function sfxHit(blockType) {\n            var ac = getAC(), t = ac.currentTime;\n            var len = Math.ceil(ac.sampleRate * 0.045);\n            var buf = ac.createBuffer(1, len, ac.sampleRate);\n            var d = buf.getChannelData(0);\n            for (var i = 0; i < len; i++) d[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, 3.5);\n            var src = ac.createBufferSource(); src.buffer = buf;\n            var filt = ac.createBiquadFilter();\n            filt.type = 'lowpass'; filt.frequency.value = 400 + blockType * 60;\n            var g = ac.createGain(); g.gain.setValueAtTime(0.09, t);\n            g.gain.exponentialRampToValueAtTime(0.0001, t + 0.045);\n            src.connect(filt); filt.connect(g); g.connect(ac.destination);\n            src.start(t);\n        }\n\n        function sfxBreak(blockType) {\n            var ac = getAC(), t = ac.currentTime;\n            var osc = ac.createOscillator(), og = ac.createGain();\n            var baseFreq = [55, 60, 58, 52, 48, 50, 44, 42, 40][Math.min(blockType, 8)];\n            osc.type = 'sine';\n            osc.frequency.setValueAtTime(baseFreq * 1.8, t);\n            osc.frequency.exponentialRampToValueAtTime(baseFreq, t + 0.18);\n            og.gain.setValueAtTime(0.45, t); og.gain.exponentialRampToValueAtTime(0.0001, t + 0.32);\n            osc.connect(og); og.connect(ac.destination); osc.start(t); osc.stop(t + 0.35);\n            var len = Math.ceil(ac.sampleRate * 0.22);\n            var buf = ac.createBuffer(1, len, ac.sampleRate);\n            var d = buf.getChannelData(0);\n            for (var i = 0; i < len; i++) d[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, 2.2);\n            var src = ac.createBufferSource(); src.buffer = buf;\n            var filt = ac.createBiquadFilter();\n            filt.type = 'lowpass'; filt.frequency.value = 300 + blockType * 40;\n            var g = ac.createGain(); g.gain.setValueAtTime(0.35, t);\n            g.gain.exponentialRampToValueAtTime(0.0001, t + 0.22);\n            src.connect(
<truncated 9688 bytes>
+++ REPLACEMENT +++
"        function sfxHit(blockType) {\n            var ac = getAC(), t = ac.currentTime;\n            var len = Math.ceil(ac.sampleRate * 0.045);\n            var buf = ac.createBuffer(1, len, ac.sampleRate);\n            var d = buf.getChannelData(0);\n            for (var i = 0; i < len; i++) d[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, 3.5);\n            var src = ac.createBufferSource(); src.buffer = buf;\n            var filt = ac.createBiquadFilter();\n            filt.type = 'lowpass'; filt.frequency.value = 400 + blockType * 60;\n            var g = ac.createGain(); g.gain.setValueAtTime(0.09, t);\n            g.gain.exponentialRampToValueAtTime(0.0001, t + 0.045);\n            src.connect(filt); filt.connect(g); g.connect(gameSoundsGainNode);\n            src.start(t);\n        }\n\n        function sfxBreak(blockType) {\n            var ac = getAC(), t = ac.currentTime;\n            var osc = ac.createOscillator(), og = ac.createGain();\n            var baseFreq = [55, 60, 58, 52, 48, 50, 44, 42, 40][Math.min(blockType, 8)];\n            osc.type = 'sine';\n            osc.frequency.setValueAtTime(baseFreq * 1.8, t);\n            osc.frequency.exponentialRampToValueAtTime(baseFreq, t + 0.18);\n            og.gain.setValueAtTime(0.45, t); og.gain.exponentialRampToValueAtTime(0.0001, t + 0.32);\n            osc.connect(og); og.connect(gameSoundsGainNode); osc.start(t); osc.stop(t + 0.35);\n            var len = Math.ceil(ac.sampleRate * 0.22);\n            var buf = ac.createBuffer(1, len, ac.sampleRate);\n            var d = buf.getChannelData(0);\n            for (var i = 0; i < len; i++) d[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, 2.2);\n            var src = ac.createBufferSource(); src.buffer = buf;\n            var filt = ac.createBiquadFilter();\n            filt.type = 'lowpass'; filt.frequency.value = 300 + blockType * 40;\n            var g = ac.createGain(); g.gain.setValueAtTime(0.35, t);\n            g.gain.exponentialRampToValueAtTime(0.0001, t + 0.22);\n            src.
<truncated 9762 bytes>